# tarja/validators/
# EN: Check-digit validators. House rules:
#   - pure functions, no external deps (stdlib only)
#   - official source of the algorithm cited at the top of each file
# PT: Validadores de DV (digito verificador). Regras da casa:
#   - funcao pura, sem dependencia externa (so stdlib)
#   - fonte oficial do algoritmo citada no topo de cada arquivo
